# Interaction catalog

The catalog now assembles component definitions from
`components/interactions/definitions.ts`.

Each interaction lives in its own folder under `components/interactions/`.
See that folder's README for the file structure and the steps for adding a
component.
